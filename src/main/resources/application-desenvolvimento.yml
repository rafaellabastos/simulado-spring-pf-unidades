spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/simulado-pf-unidade?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create
